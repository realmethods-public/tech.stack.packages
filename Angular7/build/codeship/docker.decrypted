#set( $userName = ${aib.getParam( "terraform.docker-username" )} )
#set( $password = ${aib.getParam( "terraform.docker-password" )} )
#set( $encodeStr = "$userName:$password" )
#set( $emailAddress = ${aib.getParam( "application.email" )} )
{
  "auths": {
    "https://index.docker.io/v1/": {
      "auth": "$aib.encodeString($encodeStr)",
      "email": "$emailAddress"
    }
  }
}
